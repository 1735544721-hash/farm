第 2 章 相关理论与技术

2.1 Spring Boot 框架

Spring Boot 通过自动配置与依赖管理机制降低了传统 Spring 项目搭建成本，能够快速构建基于 REST 的服务端应用。本系统在 Spring Boot 3.4.1 基础上完成接口搭建与业务逻辑组织，结合分层架构实现控制层、服务层与数据访问层的职责划分，并利用统一返回结构保证接口调用的一致性。

2.2 MyBatis-Plus 数据访问框架

MyBatis-Plus 在保留 MyBatis 灵活 SQL 能力的同时，提供了通用 CRUD、分页查询与条件构造器等功能，能够降低数据访问层的开发成本。系统使用 MyBatis-Plus 进行用户、商品、订单、库存等核心实体的增删改查，并借助分页查询实现后台列表的高效展示。

2.3 MySQL 数据库

MySQL 作为关系型数据库具备事务支持与索引优化能力，适合处理订单、库存与物流等结构化数据。本系统采用 MySQL 存储用户、商品、订单、物流与库存记录，使用主键与外键关系保证数据一致性，并通过时间字段与状态字段支撑业务过程追溯。

2.4 Redis 与本地缓存

系统引入 Redis 与 Caffeine 缓存机制，用于承载热点数据与短期查询结果，降低数据库访问压力。通过设置过期时间与容量限制，系统在高频访问场景下能够保持响应速度，适用于轮播图、公告与商品列表等展示类数据的加速。

2.5 Spring Security 与 JWT

Spring Security 提供认证与授权基础能力，能够对接口访问进行权限控制。本系统结合 JWT 完成登录态管理，使用户在多次请求中保持身份验证，并通过角色字段区分普通用户、农户与管理员权限，避免越权访问。

2.6 其他支撑技术

系统集成 Hutool 工具库用于常用工具封装，使用 Knife4j 生成接口文档并提升调试效率，同时引入邮件服务用于验证码发送与找回密码场景，并通过支付宝接口完成订单支付，形成完整的交易闭环。
